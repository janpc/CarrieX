import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  modal: {
    flex: 1,
    height: '100%',
  },
  signature: {
    flex: 1, 
    marginTop: 10,
    borderWidth: 1,
    borderRadius: 5,
    borderColor: '#BAEAEA',
    height: 250,
    backgroundColor: '#BAEAEA',
  }
});