import { StyleSheet } from "react-native";

export default StyleSheet.create({
	container: {
		flex: 1,
		padding: 20,
		paddingTop: 12,
		backgroundColor: "#fff",
	},
	deliveryContainer: {
		position: 'absolute',
		left: 0,
		right: 0,
		bottom: 40,
		paddingHorizontal: 20,
	}
});